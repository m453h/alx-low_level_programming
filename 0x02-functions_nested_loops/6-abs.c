#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @n: The input integer to display absolute value
 *
 * Return: absolute integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
